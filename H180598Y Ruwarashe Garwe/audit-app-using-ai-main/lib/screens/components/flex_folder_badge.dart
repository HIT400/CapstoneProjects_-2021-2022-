import 'package:flutter/material.dart';

class FlexBadgedFolder extends StatelessWidget {
  const FlexBadgedFolder({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}